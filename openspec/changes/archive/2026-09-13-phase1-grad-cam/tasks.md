## 1. Grad-CAM core

- [x] 1.1 Implement Grad-CAM (forward/backward hooks on `SmallCNN`'s last conv block, weighted-activation-sum + ReLU) as a standalone function taking a loaded CNN + a single 33x33x13 patch tensor, returning an 8x8 raw CAM plus its bilinear upsample to 33x33, verified by checking the CAM is non-negative everywhere and non-degenerate (not uniformly zero) on a known bosque patch — `scripts/21_grad_cam.py`, `GradCAM` class; the 3 example overlays in task 2.2 confirm non-degenerate output visually
- [x] 1.2 Verify the CAM is attributed to the *predicted* class specifically, not a fixed class, by confirming the CAM changes when computed against a different target class on the same patch — verified in `main()`: mean abs diff between CAM(predicted class) and CAM(other class) on the same patch = 0.4694, clearly class-specific
- [x] 1.3 Refuse Grad-CAM for anything other than the operational CNN (e.g. if passed a Random Forest model or its predictions), per the `grad-cam-interpretability` spec's CNN-only requirement, verified by confirming a clear refusal rather than a silent wrong result — `GradCAM.__init__` raises `TypeError` for a non-`SmallCNN` input, verified in `main()`
- [x] 1.4 Implement guided backpropagation (a modified `ReLU` backward hook zeroing negative gradients as well as negative activations) and combine it elementwise with the upsampled Grad-CAM map to produce a sharpened, still class-specific heatmap (Guided Grad-CAM), verified by confirming the sharpened map has finer spatial detail than the raw upsampled Grad-CAM while still concentrating within the same general region — `guided_backprop_saliency()` + `guided_grad_cam()`; visible in the rendered overlays (task 2.2) as per-pixel speckle rather than uniform 8x8 blocks

## 2. True-color overlay rendering

- [x] 2.1 Render the sharpened (Guided Grad-CAM) heatmap as a semi-transparent colormap overlay on the patch's true-color (B4/B3/B2, `/3000` clipped) rendering, reusing `13_visualize_classification_map.py`'s normalization convention, verified by visual inspection that the overlay aligns pixel-for-pixel with the true-color base — `render_overlay()`
- [x] 2.2 Produce this overlay for at least one example patch per class (bosque/pasto/cultivo) from the existing dataset, verified by visual inspection that each heatmap concentrates on plausible spatial evidence for its class (not spread uniformly or concentrated on an obviously irrelevant corner) — `data/study_area/gradcam_example_{bosque,pasto,cultivo}.png`; all 3 concentrate on textured canopy/soil regions within the patch, none uniform or cornered

## 3. Explaining a detected temporal transition

- [x] 3.1 Given a specific AOI pixel (row, col) flagged as a transition in `temporal-change-detection`'s change map, pull that pixel's 33x33 window fresh from Sentinel-2 for both 2019 and 2023, verified by confirming the CNN's prediction on each pulled patch matches the class already recorded in that date's saved classification raster at that pixel — `scripts/22_explain_transition.py`. **This check caught two real bugs before landing on a construction that verifies clean**: (1) `19_temporal_change_map.py`'s `pixel_to_wgs84()` used a pixel's top-left corner, not its center (~5m off) — fixed with a `+0.5` pixel offset; (2) building the query as an independent small `sampleRectangle()` at that corrected center (even reproducing `18_classify_aoi_temporal.py`'s exact UTM-box construction) still silently snapped to a different pixel grid than the tile pipeline for these boundary pixels — the fix was to stop querying independently and instead pull the same padded tile `18_classify_aoi_temporal.py` itself pulls (`classify_aoi.pull_tile`) and slice out the identical local window `classify_tile()` uses internally. After both fixes: all 4 checks (2 pixels x 2 dates) match their recorded class exactly
- [x] 3.2 Produce and render Grad-CAM overlays for both dates' patches at that pixel, side by side, verified by visual inspection against the true-color composites (same hand-picked pixels already used in `19_temporal_change_map.py`'s task 3.3 visual check, so the two checks corroborate each other) — `data/study_area/gradcam_transition_{0,1}_{2019,2023}.png`; the `pasto_a_bosque` example shows a plausible boundary case (brownish/pasto-textured true color in 2019, greener patches in 2023, Grad-CAM concentrating on exactly those greener regions in the 2023 heatmap)

## 4. Write-up

- [x] 4.1 Document the CAM resolution limitation (8x8 native, upsampled) explicitly wherever Grad-CAM outputs are presented, so it reads as a disclosed property, not a hidden imprecision — every rendered overlay's subtitle states "8x8 native, upsampled" (`render_overlay()`); mechanism and rationale documented in `GUIA_DE_ESTUDIO.md` §11

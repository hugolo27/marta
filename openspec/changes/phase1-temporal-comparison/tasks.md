## 1. Persist a reusable trained CNN checkpoint

- [x] 1.1 Add a checkpoint save/load path to `scripts/10_train_cnn.py`'s `train()` flow (or a thin wrapper), verified by training once with seed 42 and confirming a `.pt` file is written and reloadable into a fresh `SmallCNN` instance producing identical predictions on a held-out batch — `save_checkpoint()`/`load_checkpoint()` added, `main()` now persists to `data/study_area/cnn_checkpoint.pt`. Round-trip verified: a fresh `SmallCNN` loaded from the checkpoint reproduces macro F1 0.8244/accuracy 0.8217 on the full test split, matching the just-trained model's 0.824/0.822 to rounding
- [x] 1.2 Train the reference checkpoint once (seed 42, matching the seed already used as the reference throughout `phase1-classifier`) and verify its test-split accuracy matches the already-reported single-run numbers (`phase1-classifier` task 3.3) before using it for anything downstream — **exact match**: accuracy 0.822, macro F1 0.824, IoU bosque=0.883/pasto=0.593/cultivo=0.654, identical to the archived `phase1-classifier` task 3.3 reference numbers

## 2. Dense per-date classification over the AOI

- [x] 2.1 Build the 2019 and 2023 Sentinel-2 composites for the case-study AOI (`reproject=True`, `clip=True` per design.md), verified by confirming 0% residual cloud gap or an explicit reported gap fraction, not a silent assumption — `scripts/18_classify_aoi_temporal.py` run for both dates: unclassified fraction is identical to 13 decimal places across 2019 and 2023 (0.3864735119047619), meaning the entire gap is the fixed outside-AOI-polygon mask with 0% additional cloud gap in either period
- [x] 2.2 Run dense CNN inference (reusing `13_visualize_classification_map.py`'s row-chunked sliding-window approach) over each date's composite using the persisted checkpoint from 1.2, verified by producing one per-pixel class raster per date covering the full AOI extent — `aoi_classified_2019.npy`/`aoi_classified_2023.npy`, both (2800, 1200), matching the tile grid's `n_rows*tile_core_px x n_cols*tile_core_px` (7*400 x 3*400) from `aoi_classified_grid_meta.json`
- [x] 2.3 Mark pixels with no valid observation in either date as unclassified rather than imputing a class, verified by confirming the raster has an explicit no-data value distinct from the 3 real classes — value `-1`, distinct from classes `0` (bosque), `1` (pasto), `2` (cultivo); 1,298,551 px (38.6%) unclassified in both rasters

## 3. Change map and transition statistics

- [x] 3.1 Diff the two per-date rasters into a change map labeling every valid pixel as no-change or one of the 6 named pairwise transitions, verified by confirming every valid pixel gets exactly one label and unclassified pixels propagate to unclassified in the change map — `scripts/19_temporal_change_map.py`, `build_change_map()`: any pixel unclassified in either date is forced to `-1`, every other pixel gets exactly one of `no_change`/6 transition codes
- [x] 3.2 Compute and report hectares per transition type from the change map's pixel counts, verified by cross-checking the total (all transitions + no-change + unclassified) equals the AOI's known total area within rounding — sum of all categories (33,599.9 ha) matches the grid total (33,600.0 ha) to rounding; classified-only area (20,614.5 ha) matches the AOI's GEE-measured polygon area (20,589 ha, `01_verify_gee_setup.py`) within 0.1%. Only 4 of 6 possible transitions occur (`bosque_a_pasto` 1,007.4 ha, `pasto_a_bosque` 1,180.7 ha, `cultivo_a_pasto` 241.9 ha, `cultivo_a_bosque` 50.3 ha) — no transition ends in `cultivo` because the CNN found 0 cultivo pixels AOI-wide in 2023
- [x] 3.3 Produce a visualization of the change map (e.g. a categorical map with one color per transition type plus no-change), verified by visual inspection against the true-color composites for a handful of hand-picked pixels with an obvious real transition (e.g. a known cleared area) — `change_map_2019_2023.png` (categorical map) plus 2 hand-picked `pasto_a_bosque` pixels (the largest transition) rendered as 2019/2023 true-color crops: one pixel shows a visually confirmed pasture-to-forest regrowth (bare/tan patch with field lines in 2019, green cover in 2023); the other is visually ambiguous at the forest/pasture edge — consistent with design.md's noted risk of bosque/pasto boundary flicker, not a code defect
## 4. Independent sanity check

- [x] 4.1 For the same AOI pixels, extract MapBiomas' own classification for both 2019 and 2023 and compute its own transition label per pixel, verified by producing a raster in the same encoding as 3.1 — `scripts/20_mapbiomas_cross_check.py`, MapBiomas remapped directly into our bosque/pasto/cultivo/-1 scheme and diffed with the same `build_change_map()`; saved to `mapbiomas_change_map_2019_2023.npy`. Diagnostic: MapBiomas has 0 cultivo pixels in this AOI in either year (only bosque/pasto present here), and single-date CNN-vs-MapBiomas agreement is 86.1% (2019) / 86.6% (2023) — confirms the two rasters are correctly co-registered before trusting the diff-level comparison below
- [x] 4.2 Report the agreement rate between the change map's detected transitions and MapBiomas' own detected transitions, verified by a per-transition-type agreement table (not just one overall number), with an explicit note if a specific transition type shows notably low agreement (a candidate sign of classifier noise rather than real change, per design.md) — `mapbiomas_agreement.json`: `no_change` agrees at 99.3% (1,796,419/1,809,459 px), but every specific transition is far lower — `bosque_a_pasto` 0.0%, `pasto_a_bosque` 4.7%, `cultivo_a_bosque` 0.0%, `cultivo_a_pasto` 0.0% — flagged as likely classifier noise rather than confirmed real change, per design.md's predicted risk (per-date noise compounds at the diff level: both dates must independently agree with MapBiomas for a transition to match, which is a much stricter bar than the ~86% single-date agreement)
